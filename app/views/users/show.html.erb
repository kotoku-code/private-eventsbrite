<% if @user.attended_events.any? %>
	<h2>Upcoming Events</h2> 
	<ul class="media-list">
		<%= render @upcoming_events %>
	</ul>
<% else %>
	<h2>No Upcoming Events</h2>
<% end %>

<% if @events.past.any? %>
	<ul class="media-list">
		<%= render @events.past %>
	</ul>
<% end %>